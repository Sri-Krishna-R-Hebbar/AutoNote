services:
  - type: web
    name: autonote
    runtime: docker
    plan: free
    envVars:
      - key: GROQ_API_KEY
        sync: false
    disk:
      name: autonote-disk
      mountPath: /app/processed_notes
      sizeGB: 1
